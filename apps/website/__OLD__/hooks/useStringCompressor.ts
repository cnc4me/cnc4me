// import { OnChange, OnMount } from "@monaco-editor/react";
// import { compressToEncodedURIComponent, decompressFromEncodedURIComponent } from "lz-string";
// import { useMemo, useState } from "react";

// const encode = (input: string) => compressToEncodedURIComponent(input);
// const decode = (input: string) => decompressFromEncodedURIComponent(input);

// export function useStringCompressor(initial = "") {
//   return useMemo();
// }
